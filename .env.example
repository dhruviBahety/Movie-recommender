# TMDB API key — get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Set to "1" to enable Flask's debug mode (NEVER in production)
FLASK_DEBUG=0

# HTTP request timeout in seconds (default: 5)
REQUEST_TIMEOUT=5
